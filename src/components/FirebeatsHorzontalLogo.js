import React from 'react';
import {Image} from 'react-native';
import styles from './styles';

const FirebeatsHorizontalLogo = () => (
  <Image
    source={require('../assets/FireBeats-horizontal-logo.png')}
    style={styles.firebeatsHorizontal}
  />
);

export default FirebeatsHorizontalLogo;
